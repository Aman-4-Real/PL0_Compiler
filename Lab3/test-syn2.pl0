var n,a, b, c, max;
procedure P;
    const x = 0;
    procedure Q;
      	var a, b, n, sum;
        procedure  R;
 		begin
      		write(x);
 		end;
 	begin
 		read(a);
  		read(n);
  		if n > 0 then
		if n < 10 then
        begin
			b := a;
			sum := 0;
			while n>=0 do 
			begin
			sum := sum + b;
			b := b * 10 + a;
			n := n - 1;
			end;
       	end;
		write(sum);
	end;
begin
	read(a);
	read(b);
	read(c);
	if a > b then max := a
		else max := b;
	if max > c then max := max
		else max := c;
	write(max);
	call Q; 
end;

begin
    read(n);
    call P;
end.