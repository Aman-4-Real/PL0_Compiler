
var a, b, c, max;

procedure P;
begin
    read(a);
    read(b);
    read(c);
    if a > b then max := a;
    if a <= b then max := b;
    
    if max > c then max := max;
    if max <= c then max := c;

    write(max);
end;

call P.
